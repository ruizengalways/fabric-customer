# Fabric items

This directory is the home for normal customer/domain Fabric item definitions (Notebooks, Pipelines, Spark Jobs and Copy Jobs).

Certification-only Fabric items do not belong here; they live under `certification/fabric/`.

Do not create framework-version directories. Git history and immutable release identities carry version history.
